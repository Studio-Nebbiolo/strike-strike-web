# Strike-Strike 2 — web client build

Generated output only; do not edit. Built from the private `Strike-Strike` source repo (commit `5b07f8d`)
with `npm run deploy`. Play: https://studio-nebbiolo.github.io/strike-strike-web/

The client is static — it needs a game server address (menu field or `?server=wss://...`).

`maps/dust2.json` is collision/render geometry converted from Valve's `de_dust2` map;
no Valve textures or lightmaps are included. Non-commercial fan project.
